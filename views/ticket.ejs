<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Museum Entry Ticket</title>
    <style>
        body {
            font-family: 'Georgia', serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(to bottom, #e0c097, #d3b48f);
        }
        .ticket {
            width: 450px;
            height: 220px;
            border: 3px solid #8a6d3b;
            border-radius: 5px;
            background-color: #fff8e1;
            box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 20px;
            position: relative;
        }
        .ticket-header {
            text-align: center;
            font-family: 'Times New Roman', serif;
            color: #6b4f2a;
            border-bottom: 2px solid #8a6d3b;
            padding-bottom: 10px;
        }
        .ticket-header h1 {
            margin: 0;
            font-size: 24px;
        }
        .ticket-header p {
            margin: 5px 0 0;
            font-size: 16px;
            font-style: italic;
        }
        .ticket-body {
            margin: 15px 0;
        }
        .ticket-info {
            font-size: 16px;
            color: #5c432a;
            margin: 8px 0;
        }
        .ticket-footer {
            text-align: center;
            font-size: 14px;
            color: #8a6d3b;
        }
        .qr-code {
            position: absolute;
            bottom: 20px;
            right: 20px;
        }
        .qr-code img {
            width: 80px;
            height: 80px;
            border: 2px solid #8a6d3b;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <% tickets.forEach((t,i) => {  %>
    <div class="ticket">
        <div class="ticket-header">
            <h1>Magico</h1>
            <p>Exploring Art, History, and Culture</p>
        </div>
        
        <div class="ticket-body">
            
                
                
                <p class="ticket-info"><strong>Date:</strong> <%= t.date %></p>
                <p class="ticket-info"><strong>Time:</strong> <%= t.time %></p>
                <p class="ticket-info"><strong>Entry:</strong> Main Hall, Gate 2</p>
                <p class="ticket-info"><strong>Visitor:</strong> <%= t.name %></p>
                
           
                
                <div class="ticket-footer">
                <p>Thank you for visiting. Enjoy your experience!</p>
            </div>
            <div class="qr-code">
                <img src="https://via.placeholder.com/80x80.png?text=QR+Code" alt="QR Code">
            </div>
        </div>
        <% }); %>
    </div>
    </body>
</html> 